# Local Environment Example

For local development, create `.env.local` manually with these values:

```txt
NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
NEXT_PUBLIC_SUPABASE_ANON_KEY=<local Supabase anon key from `supabase status`>
```

Do not commit `.env.local`.
